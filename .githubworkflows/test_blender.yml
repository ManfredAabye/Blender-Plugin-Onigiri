import bpy
from .. import rigs  # Import aus Plugin-Root

def test_rig_creation():
    bpy.ops.object.armature_add()
    result = rigs.create_custom_rig()  # Beispiel-Funktion
    assert result == 'SUCCESS'